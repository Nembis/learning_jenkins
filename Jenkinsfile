pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building'
                sleep 2
                echo 'Done Building'
            }
        }

        stage('Uploading') {
            steps {
                echo 'Uploading'
                sleep 2
                echo 'Done Uploading'
            }
        }
    }
}
